<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver
            android:name=".SlideshowControlWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/slideshow_control_info" />
        </receiver>

        <receiver android:name=".SlideshowControlReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="io.github.doubi88.slideshowwallpaper.PREVIOUS_IMAGE" />
                <action android:name="io.github.doubi88.slideshowwallpaper.NEXT_IMAGE" />
                <action android:name="io.github.doubi88.slideshowwallpaper.OPEN_IMAGE" />
            </intent-filter>
        </receiver>
        <service
            android:name=".SlideshowWallpaperService"
            android:enabled="true"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_WALLPAPER">
            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />
            </intent-filter>

            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/slideshow_wallpaper" />
        </service>

        <activity
            android:name=".preferences.WallpaperPreferencesActivity"
            android:exported="true"
            android:theme="@style/PreferenceTheme">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity
            android:name=".infoScreen.InfoScreenActivity"
            android:exported="false"
            android:label="@string/preference_info"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".preferences.imageList.ImageListActivity"
            android:exported="false"
            android:label="@string/image_list_name"
            android:theme="@style/AppTheme" />
    </application>

</manifest>